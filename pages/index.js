function Home(){
    return <h1>Pai, Mãe, amo MUITO vocês</h1>
}

// A exportação padrão é responsável por exportar a estrutura principal, que pode posteriormente conter mais itens como imagens, botões, etc..
export default Home;

// Para abrir o site na WEB --> Clicar no ícone de antena no canto inferior esquerdo e trocar a propriedade "visibility" para "public".
// Caso a aplicação seja executada localmente -> Navegador --> localhost:3000